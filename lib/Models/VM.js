// Generated by CoffeeScript 1.8.0
(function() {
  var Promise, debug;

  debug = require('debug')('XenAPI:VM');

  Promise = require('bluebird');

  module.exports = (function() {
    var VM, key, session, vm;

    key = void 0;

    session = void 0;

    vm = void 0;

    VM = void 0;

    function exports(_session, _vm, _key) {
      debug("constructor()");
      if (!_session) {
        throw Error("Must provide `session`");
      } else {
        session = _session;
      }
      if (!_vm) {
        throw Error("Must provide `vm`");
      }
      if (!(!_vm.is_a_template && !_vm.is_control_domain && _vm.uuid)) {
        throw Error("`vm` does not describe a valid VM");
      } else {
        vm = _vm;
      }
      if (!_key) {
        throw Error("Must provide `key`");
      } else {
        key = _key;
      }
      this.uuid = vm.uuid;
      this.name = vm.name_label;
      this.description = vm.name_description;
      this.xenToolsInstalled = !(vm.guest_metrics === 'OpaqueRef:NULL');
      this.powerState = vm.power_state;
      this.POWER_STATES = {
        HALTED: 'Halted',
        PAUSED: 'Paused',
        RUNNING: 'Running',
        SUSPENDED: 'Suspended'
      };
    }

    return exports;

  })();

}).call(this);
